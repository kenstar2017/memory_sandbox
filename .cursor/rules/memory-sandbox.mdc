---
description: 每轮先 memory_prepare；参考问答结合仓库；凡 AI 对话结论都必须 memory_remember
alwaysApply: true
---

# 记忆沙箱接入规则

## 每轮必做（硬性）

在回答用户之前（纯记忆管理指令除外），**必须先调用** MCP 工具 `memory_prepare`，参数 `query` 用用户原话：

1. 工具会把问题拼成 `xxxx，记录到长期记忆。`（用户已手写该后缀则不重复拼）
2. **只检索**本地感觉/工作/长时记忆，**不会**调用记忆沙箱内的 LLM
3. 返回里通常含 `references` / `context_pack`（多条软召回参考问答）与可选硬命中 `answer`
4. 未硬命中或改代码时：由**当前 AI 工具自己的模型**结合参考与仓库继续；结束后 `memory_remember`

可跳过 `memory_prepare` 的纯管理指令示例：备份/清空/恢复/删除单条记忆、查看记忆状态、切换场景。

## 命中 / 参考 / 未命中

1. 若返回 `references` 或 `context_pack`（有内容）：
   - **必须纳入本轮推理**，与当前仓库、用户上下文一起用
   - 参考可能过时，**以仓库/现状为准**，不要整段照抄当最终实现
2. 若在改功能 / 查代码 / 排障：
   - **不要**因 `hit_local=true` 而短路结束
   - 把沙箱问答当背景参考，再读代码、给方案或改文件
   - 本轮新结论结束前 `memory_remember`
3. 若问题只是纯事实复述、无需改代码，且 `hit_local=true`：
   - 可优先采用沙箱的 `answer` 回答用户
   - **不要**用模型默认身份覆盖沙箱答案
   - 可简短注明「来自记忆沙箱」；仍建议扫一眼 `references` 是否冲突
4. 若 `hit_local=false` 且无参考：
   - 以返回的 `assembled` 作为待解决问题继续推理 / 查代码 / 排障

## 结束前必须写入（硬性 · 含排障与改代码）

**凡是本轮 AI 对话产生的可复用结论，结束前都必须调用 `memory_remember`。**

包括但不限于：

- 排障过程与根因、修复步骤
- 代码改动说明、配置变更、命令与路径
- 方案选型、踩坑结论、环境注意点
- 未命中后新推出来的答案

写法：

- `question`：精简问法或用户 `original`（不要只写「帮我看看」）
- `answer`：稳定结论（原因 + 做法 + 关键路径/命令）
- `scene`：可用 `dev`
- `tags`：可选，如 `["feishu","frontend"]`；也可在 question 写 `#tag`
- `kind` / `facts`：可选结构化类型（command/path/env/pitfall/decision）
- 命中时可读 `hits` / `explain` / `references`（分数与原因），便于纠错或 `forget`
- 终端/日志可先 `memory_extract` 得候选，确认后再 `memory_remember`（写入自动脱敏）
- 知识包分享：`memory_export_pack` / `memory_import_pack`；很久不用的用 `memory_archive`
- 改完代码可 `memory_git_check` 扫过时记忆；飞书文档可用 `memory_feishu_bookmark` 再确认写入

例外（可不写）：

- 纯寒暄且无信息量
- 用户明确说「不要记住」
- 本轮只做了备份/清空/删除等记忆管理指令本身
- 密钥 / token / 密码（永远不要写入）

纯命中且本轮无新信息：仍应用 `memory_remember` 用同一问答再写一次以强化命中（允许去重更新）。

## 其它工具

- 需要原样检索、不自动拼接后缀 → `memory_ask`（同样返回 `references` / `context_pack`）
- 用户要忘记内容 → `memory_forget`（清空长时/全部时必须 `confirm=true`，建议先 `memory_backup`）
- 用户要备份长时记忆 → `memory_backup`
- 切换项目/环境 → `memory_set_scene`
- 查看状态 → `memory_status`
