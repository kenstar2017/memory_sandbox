---
description: 每轮先 memory_prepare；参考问答结合仓库；凡 AI 对话结论都必须 memory_remember
alwaysApply: true
---

# 记忆沙箱接入规则

## 每轮必做（硬性）

在回答用户之前（纯记忆管理指令除外），**必须先调用** MCP 工具 `memory_prepare`，参数 `query` 用用户原话：

1. 工具会把问题拼成 `xxxx，记录到长期记忆。`（用户已手写该后缀则不重复拼）
2. **只检索**本地感觉/工作/长时记忆，**不会**调用记忆沙箱内的 LLM
3. 返回里通常含 `references` / `context_pack`（多条软召回参考问答）与可选硬命中 `answer`
4. 未硬命中或改代码时：由**当前 AI 工具自己的模型**结合参考与仓库继续；结束后 `memory_remember`

可跳过 `memory_prepare` 的纯管理指令示例：备份/清空/恢复/删除单条记忆、查看记忆状态、切换场景。

## 命中 / 参考 / 未命中

1. 若返回 `references` 或 `context_pack`（有内容）：
   - **必须纳入本轮推理**，与当前仓库、用户上下文一起用
   - 参考可能过时，**以仓库/现状为准**，不要整段照抄当最终实现
2. 若在改功能 / 查代码 / 排障：
   - **不要**因 `hit_local=true` 而短路结束
   - 把沙箱问答当背景参考，再读代码、给方案或改文件
   - 本轮新结论结束前 `memory_remember`
3. 若问题只是纯事实复述、无需改代码，且 `hit_local=true`：
   - 可优先采用沙箱的 `answer` 回答用户
   - **不要**用模型默认身份覆盖沙箱答案
   - 可简短注明「来自记忆沙箱」；仍建议扫一眼 `references` 是否冲突
4. 若 `hit_local=false` 且无参考：
   - 以返回的 `assembled` 作为待解决问题继续推理 / 查代码 / 排障

## 结束前必须写入（硬性 · 含排障与改代码）

**凡是本轮 AI 对话产生的可复用结论，结束前都必须调用 `memory_remember`。**

包括但不限于：

- 排障过程与根因、修复步骤
- 代码改动说明、配置变更、命令与路径
- 方案选型、踩坑结论、环境注意点
- 未命中后新推出来的答案

写法：

- `question`：精简问法或用户 `original`（不要只写「帮我看看」）
- `answer`：稳定结论（原因 + 做法 + 关键路径/命令）
- `scene`：可用 `dev`
- `tags`：可选，如 `["feishu","frontend"]`；也可在 question 写 `#tag`
- `kind` / `facts`：可选结构化类型（command/path/env/pitfall/decision）
- 命中时可读 `hits` / `explain` / `references`（分数与原因），便于纠错或 `forget`
- 终端/日志可先 `memory_extract` 得候选，确认后再 `memory_remember`（写入自动脱敏）
- 知识包分享：`memory_export_pack` / `memory_import_pack`；很久不用的用 `memory_archive`
- 改完代码可 `memory_git_check` 扫过时记忆；飞书文档可用 `memory_feishu_bookmark` 再确认写入

例外（可不写）：

- 纯寒暄且无信息量
- 用户明确说「不要记住」
- 本轮只做了备份/清空/删除等记忆管理指令本身
- 密钥 / token / 密码（永远不要写入）

纯命中且本轮无新信息：仍应用 `memory_remember` 用同一问答再写一次以强化命中（允许去重更新）。

## 强制门禁 hook（用户级 · 全项目生效）

只靠规则提醒不可靠：AI 会忘着查（凭空写文档，而模板早就在记忆里），也会忘着记
（「读需求 → 调研 → 写技术文档」做完就结束了）。所以读写两侧都加了机械门禁，装在
**用户级** `~/.cursor/hooks.json` + `~/.cursor/hooks/`，在任何项目里都生效：

| 脚本 | 事件 | 作用 |
| --- | --- | --- |
| `memory-session-context.py` | `sessionStart` | 用 `additional_context` 把本协议注入每个会话的初始系统上下文 |
| `memory-require-prepare.py` | `preToolUse` | 本轮没查过记忆就动手改东西 → 拦一次，要求先 `memory_prepare` |
| `memory-mark.py` | `postToolUse` | 调过 prepare/ask 写 `.prepared`，调过 remember 写 `.remembered` |
| `memory-ensure-remember.py` | `stop` | 没落库就追问一轮；并按轮清掉读侧标记、清理过期状态 |

**改 hook 要改仓库里的源**：脚本源在 `cursor_hooks/`，安装逻辑在 `core/cursor_hooks.py`，
装到用户机器上靠 `python3 main.py hooks-install`（或 BloomBox 工具栏「AI 门禁」）。
不要直接手改 `~/.cursor/hooks/` 下的副本——那是产物，下次安装会被覆盖，且别人装不到。
脚本**只准用标准库**（有单测卡着），否则装到别人机器上会 import 失败。

要点：

- 标记按 `conversation_id` 存在 `~/.cursor/memory-sandbox-hook-state/`，每轮 `stop` 清一次，
  所以是「每轮都要查、每轮都要记」，不是一个会话里查一次就永久放行
- 读侧只拦 `Write` / `Delete` / `Task` 与飞书写操作；`Read` / `Grep` / `Shell` 和所有
  `memory_*` 工具照常放行，探索速度不受影响
- 同一轮最多拦一次（`.gated` 标记）：MCP 挂了或确实无关时不会把整轮卡死
- `stop` 配 `loop_limit: 1` 只追问一次；追问那一轮**保留**读侧标记，免得补记时又被拦
- 所有脚本失败放过（打印 `{}` / `permission: allow` 并 exit 0），绝不挡住正常工作流

被 `preToolUse` 拦下时：先 `memory_prepare`（query 用用户原话），再重试刚才那个操作。
被 `stop` 追问时：本轮确有可复用结论就补写；确实没有（纯寒暄、纯记忆管理指令）回一句说明即可，
不要为了交差写垃圾记忆。

两个历史坑，别踩回去：

1. 这套东西原先放在本仓库 `.cursor/hooks.json`，是**项目级**的，只在 memory_sandbox 仓库生效；
   在别的项目里工作时完全没有门禁，结论就这么丢了。已挪到用户级，仓库内那份已删除，
   别再加回来——两份都在会重复执行（同一轮被追问两次）。
2. 别指望 `beforeSubmitPrompt` 注入上下文：它只认 `continue` / `user_message`，多余字段会被
   静默忽略，日志里看着像成功却毫无效果。`sessionStart` 的 `additional_context` 才是可用的注入点。

## 改动飞书文档：必须逐次取得本人确认（硬性）

飞书文档多为团队共享内容，改动别人看得见、且不易回滚。因此：

1. **禁止自行发起任何飞书写操作**（新建文档、改标题、改正文、**加评论/回复评论**、移动/删除
   节点等）。必须先在对话里说明「改哪一篇、从什么改成什么」或「要新建什么、建在哪」，
   等用户明确同意后才执行。评论会通知文档协作者、别人立刻看得见，同样算写操作。
2. **不得自行使用 `--yes`**，也不得自行传 `confirmed=True` 绕过确认。这两个开关只在
   用户本轮已明确同意时才可使用；用户没说，就一律先问。
3. 「用户提过这篇文档」「上一轮已经批准过一次」都**不构成**本次的确认，每次改动都要重新确认。
4. 只读操作（拉正文、读标题、`memory_feishu_preview`、`memory_feishu_bookmark`）不受此限。
5. 代码侧门禁：`core/feishu.py` 的写函数 `confirmed` 默认 `False` 且默认拒绝，
   新增飞书写能力时必须沿用该参数，不要提供绕过路径。

可用的飞书工具（MCP 与 CLI 等价，两条路都要确认）：

| 用途 | MCP 工具 | CLI |
| --- | --- | --- |
| **读正文**（看/分析/引用文档内容） | `memory_feishu_read` | — |
| 只读预览标题与正文块数（不含正文） | `memory_feishu_preview` | — |
| 读评论（看评审意见） | `memory_feishu_list_comments` | `feishu-comments` |
| 加评论（局部 / 全文）、回复评论（写操作） | `memory_feishu_comment` | `feishu-comment` |
| 新建文档（可带正文） | `memory_feishu_create_doc` | `feishu-create-doc` |
| 改正文（append / replace） | `memory_feishu_edit_body` | `feishu-edit-body` |
| 改 wiki 标题 | `memory_feishu_set_title` | `feishu-set-title` |

要文档内容就用 `memory_feishu_read`（返回纯文本正文，超长时按 `next_offset` 续读）。
`memory_feishu_preview` 只给标题与块数、**不含正文**，别拿它当读正文的工具；
`memory_feishu_bookmark` 是把文档转成待确认记忆候选、正文会被截断到约 1200 字，也不是读正文用的。

改正文前先用 `memory_feishu_preview` 确认目标文档与块数，把「改哪一篇、追加还是替换、会删多少块」
讲清楚再请用户确认。`mode=replace` 会真的删掉原有块（飞书侧可用历史版本恢复），尤其不可自行发起。

评论分两种，**默认优先局部评论**：

- 传 `anchor_text`（CLI `--on`）= 局部评论（划词评论），锚定到具体段落、在正文旁边显示并带引用
- 都不传 = 全文评论，显示在文档**最底部**（用户常因此以为「评论没发出去」）
- 传 `comment_id` = 回复已有评论；不能与 `anchor_text` / `block_id` 同时用

审阅、校对、逐条指出问题时**必须逐个问题发局部评论**，不要把多条意见合并成一条全文评论。
`anchor_text` 取一小段连续且独特的原文即可；命中多个段落会直接报错并列出候选，这时换更独特的
片段或改用 `block_id`——不要为了发出去而随便挑一个。

正文按 Markdown 写即可，**表格和行内粗体都会正常渲染**，不必手工改写成列表来规避：

- 块级：`#`~`######`、`-`/`*`、`1.`、``` 代码块、`>`、`---`、GFM 表格（`| a | b |` 加 `|---|---|` 分隔行）
- 行内：`**粗体**`、`*斜体*`、`~~删除线~~`、`` `代码` ``、`[文字](链接)`
- 代码块与行内代码里的 `**`、`|` 保持字面量；不支持图片、脚注、任务列表、单元格内换行
- 表格是 table → table_cell → 文本 三层嵌套，写入时单独走「创建嵌套块」接口，
 与平铺块按原文顺序交替追加，所以混排顺序不会乱
- 列宽按每列最长内容自动分配（总宽约 800px，每列保底 100px），长文件路径不会再被挤成一列一个字，
 不需要为了排版把表格拆成列表

**写成功后会自动落一条「操作记录」**：新建/改正文/改标题一旦在飞书侧生效，就自动写一条
长时记忆（问法 `《标题》飞书文档正文与写入记录 <链接>`，tags 带 `doc-write`）。返回里的
`remembered` 字段就是落库结果，不必再为「这次改了哪篇文档」重复写一遍。同一篇文档反复改
会更新同一条，不会堆积。

但那条只有链接、大纲和开头约 600 字摘录，**它记的是「我写过这篇文档」，不是文档里的结论**。
需求的字段模型与口径、调研结论、评审发现的问题、方案取舍，仍必须各自 `memory_remember`
单独落库；把它当成「本轮已经记过了」是错的——否则下次只能召回一个链接，而文档还会被别人改。

## 其它工具

- 需要原样检索、不自动拼接后缀 → `memory_ask`（同样返回 `references` / `context_pack`）
- 用户要忘记内容 → `memory_forget`（清空长时/全部时必须 `confirm=true`，建议先 `memory_backup`）
- 用户要备份长时记忆 → `memory_backup`
- 切换项目/环境 → `memory_set_scene`
- 查看状态 → `memory_status`
